spring:
  datasource:
    url: jdbc:h2:mem:resume-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true

spring.servlet.multipart:
  max-file-size: 5MB
  max-request-size: 5MB
